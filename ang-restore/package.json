{
  "name": "snm-restore",
  "version": "0.0.0",
  "main": "main.js",
  "scripts": {
    "ng": "ng",
    "electron": "run-s build:web build:electron",
    "electron-aot": "run-p build:web-aot build:electron",
    "build:web": "cd web && ng build",
    "build:web-aot": "cd web && ng build --prod",    
    "build:electron": "electron ."
  },
  "private": true,
  "dependencies": {
    "npm-run-all": "^4.1.3",
    "ts-node": "~7.0.0",
    "tslint": "~5.11.0",
    "typescript": "~2.9.2"
  },
  "devDependencies": {
    "electron": "^3.0.3"
  }
}
